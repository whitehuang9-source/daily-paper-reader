Title: Nonlinear RMM-GKS for Large-Scale Dynamic and Streaming Inverse Problems with Uncertain Forward Operators

URL Source: https://arxiv.org/pdf/2605.06336v1

Published Time: Fri, 08 May 2026 01:43:01 GMT

Number of Pages: 34

Markdown Content:
NONLINEAR RMM-GKS FOR LARGE-SCALE DYNAMIC AND STREAMING INVERSE PROBLEMS WITH UNCERTAIN FORWARD OPERATORS 

TOLUWANI OKUNOLA ∗, MIRJETA PASHA † , MISHA E. KILMER ‡ , JAMES G. NAGY § , 

> AND

ERIC DE STURLER ¶

Abstract. Many practical imaging systems suffer from uncertainty in acquisition geometry— such as projection angles in computed tomography or sensor positions in photoacoustic tomography— leading to nonlinear inverse problems that require joint estimation of both the image and the for-ward model parameters. Standard approaches that assume a known linear forward operator fail to account for these uncertainties, resulting in significant reconstruction artifacts. We propose a non-linear recycled majorization–minimization generalized Krylov subspace (NL-RMM-GKS) framework for large-scale inverse problems with uncertain forward operators. The method extends MM-GKS to nonlinear settings by combining majorization–minimization for nonsmooth regularization with Krylov subspace projection and subspace recycling, ensuring bounded memory usage. Two complementary formulations are developed: an alternating minimization approach that al-ternates between image updates and Gauss–Newton parameter estimation, and a variable projection approach that eliminates the image variable and optimizes directly over the parameters using inex-act inner solves. We further introduce streaming variants that process data sequentially, enabling reconstruction from large or dynamically acquired datasets without storing the full operator. For dynamic problems, we incorporate two temporal regularization strategies—optical flow and aniso-tropic total variation—as plug-in choices within the framework. We carry out rigorous numerical experiments in fan-beam computed tomography and photoacoustic tomography to demonstrate that our proposed framework achieves high-quality reconstructions with bounded memory requirements, making it suitable for large-scale dynamic imaging problems. 

Key words. Nonlinear inverse problems, dynamic tomography, generalized Krylov subspace, recycling methods, streaming reconstruction, edge-preserving regularization, variable projection, un-certain geometry 

AMS subject classifications. 65F22, 65F10, 65J20, 65K10, 68U10 

1 Introduction. Inverse problems arise throughout science and engineering when one seeks to determine unknown parameters or images from indirect measure-ments. In medical imaging, deblurring, geophysical exploration, and astronomical observation, the challenge is that of reconstructing an unknown image u ∈ Rn from noisy measurements b ∈ Rm related through a forward operator. In many practical scenarios, the forward operator depends on unknown parameters p ∈ Rnp , leading to a nonlinear relationship: (1.1) H(p)u + e = b,

where H(p) ∈ Rm×n represents the forward operator, e denotes measurement noise, and p encodes geometric or calibration uncertainties. Consider computed tomography where projection angles or source locations may be imperfectly calibrated, photoa-coustic imaging where sensor positions contain uncertainties, or deblurring problems where the point spread function is not precisely known. This problem is often un-derdetermined or ill-posed, making the least-squares solution non-unique or highly 

> ∗

Department of Mathematics, Tufts University (toluwani.okunola@tufts.edu) 

> †

Department of Mathematics, Virginia Tech (mpasha@vt.edu) 

> ‡

Department of Mathematics, Tufts University (misha.kilmer@tufts.edu) 

> §

Institute of Mathematics, Emory University (jnagy@emory.edu) 

> ¶

Department of Mathematics, Virginia Tech (sturler@vt.edu) 1

> arXiv:2605.06336v1 [math.NA] 7 May 2026

2 OKUNOLA T., PASHA M., KILMER M., NAGY J. DE STURLER E. 

sensitive to noise. To combat these issues, regularization is employed, leading to the joint estimation problem: (1.2) min  

> u,p

12 ∥H(p)u − b∥22 + λ∥Ψu ∥qq ,

where 0 < q ≤ 2, Ψ represents a regularization operator, and λ > 0 is a regulariza-tion parameter. We are thus presented with the joint tasks of estimating both the image u and the geometric or calibration parameters p. This joint estimation prob-lem is significantly more challenging than the linear case, as it couples the typically large-dimensional image reconstruction problem with a smaller but highly nonlin-ear parameter estimation problem. The challenge becomes even more pronounced in 

dynamic imaging settings, where the object itself changes over time—as in cardiac CT or time-lapse photoacoustic imaging—and the forward operator may simulta-neously depend on unknown geometric parameters. In such cases, temporal regu-larization must be incorporated alongside joint image-parameter estimation, further coupling an already challenging reconstruction problem. Alternative approaches in-clude Bayesian formulations with spatiotemporal priors that promote edge preser-vation through wavelet-based representations [15], and sparse Bayesian learning ap-proaches that embed sparsity-promoting priors into Krylov subspace solvers [18]. In the case of image reconstruction, q in Equation (1.2) is often set to 1 in order to ensure that the regularization term is a valid norm [4, 6, 8]. The effect of this choice depends on Ψ: when Ψ is a gradient or finite difference operator, q = 1 promotes edge preservation (akin to total variation regularization), whereas when Ψ is the identity, it promotes sparsity in the solution. We will therefore use q = 1 throughout the paper, but the framework we present can easily be generalized to other values of q.In the special case where the forward operator is fully known ( p fixed), (1.1) reduces to the classical linear model Hu + e = b, and (1.2) reduces to the standard regularized least-squares problem: (1.3) min 

> u

12 ∥Hu − b∥22 + λ∥Ψu ∥qq ,

which has been extensively studied in the literature [4, 6, 8]. 

1.1 Existing Approaches and Their Limitations. The challenge of recon-structing images when the forward operator contains unknown parameters has been addressed through several distinct approaches. Block coordinate descent (BCD) meth-ods, also known as alternating minimization, alternate between fixing one variable set and optimizing over the other, converting the joint minimization into a sequence of simpler subproblems [2, 26]. However, BCD is sensitive to initialization and tends to converge to local minima [19]. Variable projection (VarPro), introduced by Golub and Pereyra [12, 13], offers an elegant alternative by eliminating the image variable analytically. Given a separable 

ℓ2 problem where measurements depend linearly on the image u and nonlinearly on parameters p, VarPro substitutes u = H(p)†b to obtain a reduced objective depending only on p. It has been shown in the literature (see [14, 24, 10]) that the VarPro approach of separating the linear variable u from the nonlinear variable p

speeds up the convergence of iterative methods used to solve (1.2) in the case q = 2. The case 1 ≤ q < 2 which we consider here is more challenging, as there is no closed-form expression for the solution u(p) of the problem for fixed p (see [9]). More recently, machine learning and optimization-hybrid methods have emerged for joint calibration and reconstruction in nonlinear inverse problems. Here we use NL-RMM-GKS FOR DYNAMIC AND STREAMING INVERSE PROBLEMS 3the term “hybrid” to mean a combination of learned and iterative optimization com-ponents, distinct from the hybrid Krylov methods used for linear regularization [6]. Machine learning models have been trained to predict appropriate regularization pa-rameters directly [1, 7]. Inspired by this, Meng et al. [19] introduce a framework that calibrates geometry parameters using a neural network, and then refines the predicted parameters and reconstructs the imaged object simultaneously using BCD. This two-stage strategy aims to combine the speed and generalization of learned models with the reliability and interpretability of optimization-based refinement. However, such hybrid methods inherit the limitations of their constituent parts—they still rely on BCD for the refinement stage with its associated convergence issues, and the learned components require substantial training data and may not generalize well to out-of-distribution scenarios or geometries not seen during training. Furthermore, the learning phase introduces an additional layer of complexity that can be difficult to implement in practice. For the linear case ( p known), the majorization-minimization generalized Krylov subspace (MM-GKS) method introduced by Lanza et al. [17] has proven highly ef-fective for solving large-scale inverse problems [3, 23]. The algorithm addresses the non-smooth ℓ1-type regularization by constructing a sequence of quadratic majorants, each of which can be minimized efficiently using projection onto a small generalized Krylov subspace. MM-GKS tackles problems of the form (1.3) by iteratively refining a weighted quadratic approximation to the objective, automatically selecting the regu-larization parameter and solving the quadratic problem on small projected subspaces. However, MM-GKS in its original form has two significant limitations for large-scale and nonlinear problems. First, the Krylov subspace dimension grows with each iteration, requiring storage of an expanding basis. For problems requiring hundreds of iterations to converge, this becomes prohibitively costly. Second, the method as-sumes a fixed, known forward operator H, and cannot directly handle the nonlinear dependence on parameters p in equation (1.2). Recent developments have partially addressed the memory limitation. In [5], a restarted MM-GKS algorithm is introduced which periodically restarts the algorithm to reduce memory and computational costs. However, after restarting, their approach ignores the previous solution subspace, discarding relevant information that has been collected. The recycled MM-GKS (RMM-GKS) method introduced by Pasha et al. in [21] alternates between enlarging the Krylov subspace and compressing it to re-tain only the most informative directions. This keeps the memory bounded while maintaining convergence properties. The authors also introduce a streaming variant (s-RMM-GKS) that further extends this to sequential data processing, accommodat-ing scenarios where measurements arrive in blocks or where the full dataset cannot be stored due to limited memory. For dynamic problems with known geometry, the MM-GKS framework has been combined with optical flow regularization (MMGKS-OF) [20], which alternates between estimating forward and reverse optical flow velocity fields and reconstructing the image sequence, incorporating the estimated motion as a linearized regularization operator within each MM-GKS solve. A unified framework for edge-preserving dynamic reconstruction using spatio-temporal anisotropic total variation regularization—which simultaneously promotes sparse spatial gradients per frame and penalizes inter-frame differences via a joint ℓ1 penalty—was developed in [23]. Despite these advances, however, the fundamental challenge of nonlinear geometry remains unaddressed within the MM-GKS framework. When the forward operator depends on unknown parameters, existing methods either treat the parameters as 4 OKUNOLA T., PASHA M., KILMER M., NAGY J. DE STURLER E. 

fixed, accepting the resulting artifacts, or alternate between image reconstruction and parameter estimation using generic nonlinear solvers that discard valuable Krylov subspace information after each outer iteration. None of these approaches fully lever-age the structure of the problem or the efficiency of Krylov-based methods, which we address in the present work. 

1.2 Contributions. In this paper, we develop a comprehensive framework for nonlinear inverse problems with uncertain forward operators that extends and unifies previous MM-GKS variants. Our contributions include: 

Nonlinear RMM-GKS framework (NL-RMM-GKS). We present two complemen-tary realizations for joint image-parameter estimation:  

> ■

An alternating minimization (AltMin) approach that majorizes the non-smooth penalty and alternates between updating the image via recycled Krylov projections and updating parameters through Gauss-Newton steps.  

> ■

A variable projection (VarPro) approach that eliminates the image vari-able through an inner MM-GKS solve and performs Gauss-Newton optimiza-tion on the reduced objective. Both realizations share the same inner MM-GKS machinery, regularization strate-gies, and parameter selection methods, differing only in how they handle parameter updates. 

Recycling for nonlinear problems. We adapt the enlarge-compress recycling strat-egy to the nonlinear setting, maintaining bounded memory while preserving essential search directions. The recycling mechanism intelligently retains information about the solution structure even as the forward operator changes with parameter updates. 

Streaming extensions (s-NL-RMM-GKS). We develop streaming variants that process measurement blocks sequentially, carrying over basis information between blocks. This enables reconstruction from data that arrives sequentially or from datasets too large to fit in memory. 

Temporal regularization for dynamic problems. For dynamic imaging sequences, we incorporate two plug-in temporal regularization strategies within the NL-RMM-GKS framework: optical flow regularization [20], which enforces a physics-based mo-tion model between frames, and anisotropic total variation [21], which penalizes frame-to-frame differences without assuming a specific motion structure. 

Unified methodology. Our framework naturally generalizes MMGKS, RMM-GKS, s-RMM-GKS, and MMGKS-OF as special cases, providing a unified formulation of these methods. 

1.3 Organization. The remainder of this paper is organized as follows. In Section 2 we establish notation and review essential background on inverse problems and regularization. In Section 3 we summarize the MM-GKS method and its recycling and streaming variants for linear problems. Our main contributions are presented in Section 4: the nonlinear RMM-GKS framework with both AltMin and VarPro realizations, convergence analysis, and detailed algorithms. Streaming extensions of NL-RMM-GKS are developed in Section 5. Section 6 contains static numerical experiments in computed tomography and photoacoustic tomography. The extension to dynamic problems, including the temporal regularization strategies and dynamic experiments, is presented in Section 7. We conclude in Section 8 with a summary and discussion of future directions. A proof of the main convergence result is provided in Appendix A. NL-RMM-GKS FOR DYNAMIC AND STREAMING INVERSE PROBLEMS 5

2 Preliminaries. We start by introducing our notation, the regularization used in the specification of the problem, and the temporal regularization strategies employed for dynamic imaging. 

2.1 Notation and Problem Setup. We denote vectors by lowercase bold letters ( u, b, p) and matrices by uppercase bold letters ( H, Ψ). For dynamic problems involving multiple time frames, we stack the images as u = vec([ u(1) , . . . , u(nt)]), where each u(j) ∈ Rns represents a single frame and n = nsnt is the total dimension. The forward operator H(p) ∈ Rm×n maps the image space to the measurement space and depends on parameters p ∈ Rnp , which encode geometric or calibration in-formation such as perturbations in projection angles or detector positions in computed tomography, radial shifts in photoacoustic imaging, or point spread function param-eters in deblurring. The measurement vector b ∈ Rm contains noisy observations related to the true image through equation (1.1). 

2.2 Regularization Operator. For static problems, Ψ represents a finite dif-ference approximation to the gradient in horizontal and vertical directions. For dy-namic problems, we include temporal differences: (2.1) Ψ =

Int ⊗ Iny ⊗ Lx

Int ⊗ Ly ⊗ Inx

Lt ⊗ Iny ⊗ Inx

 ,

where Lx, Ly , Lt are discrete derivative operators in the horizontal, vertical, and tem-poral directions respectively, and ⊗ denotes the Kronecker product. The choice of how the temporal component Lt is constructed determines the temporal regulariza-tion strategy, as described in Section 2.3 below. 

2.3 Temporal Regularization for Dynamic Problems. For dynamic imag-ing problems, the choice of temporal regularization strategy significantly affects re-construction quality. We consider two plug-in strategies within the NL-RMM-GKS framework: anisotropic space-time total variation (ANISO-TV) [23], which promotes sparse spatial gradients and penalizes frame-to-frame intensity differences, and optical flow regularization (OF) [20], which enforces a physics-based motion model between frames. Both strategies are described in full detail in Section 7, where their mathe-matical formulations and algorithms are presented alongside the dynamic numerical experiments. 

2.4 Streaming Data and Block Processing. For streaming applications, we partition the forward operator and measurement vector into N blocks: (2.2) H(p) = 



H1(p)...

HN (p)

 , b =



b1

...

bN

 ,

where each Hj (p) ∈ Rmj ×n and bj ∈ Rmj corresponds to a randomly selected subset of measurements (e.g., a randomly assigned block of projection angles in CT). The measurements are partitioned by drawing without replacement, so that each mea-surement appears in exactly one block. The streaming algorithms process block j

in sequence, updating the solution and parameter estimates while carrying over the compressed Krylov basis from block j −1 to block j. The number of blocks N controls the memory-quality trade-off: larger N reduces peak memory and runtime but may degrade reconstruction quality, a trade-off we explore systematically in Section 6. 6 OKUNOLA T., PASHA M., KILMER M., NAGY J. DE STURLER E. 

3 Background: MM-GKS and Related Methods. Here we review the MM-GKS framework for linear inverse problems [17, 16] and its extensions to recy-cling and streaming from [21]. These methods form the foundation for our nonlinear algorithms. 

3.1 Majorization-Minimization Approach. The ℓ1 regularization in equa-tion (1.3) is non-differentiable at zero, making standard gradient-based optimization challenging. The majorization-minimization (MM) approach addresses this by con-structing a sequence of smooth quadratic approximations to 

J (u) := 12 ∥Hu − b∥22 + λ∥Ψu ∥1.

Given a current iterate u(k), we define z(k) = Ψu (k) and construct diagonal weights: (3.1) P(k) 

> ϵ

= diag 

q 

(z(k) 

> i

)2 + ϵ2

−1/2

,

where ϵ > 0 ensures differentiability. These weights give rise to a quadratic majorant: (3.2) Q(u | u(k)) = 12 ∥Hu − b∥22 + λ

2 ∥P(k) 

> ϵ

Ψu ∥22 + c, 

where c is a constant independent of u. This majorant satisfies: (i) Q(u(k) | u(k)) = 

J (u(k)), (ii) ∇Q (u(k) | u(k)) = ∇J (u(k)), and (iii) Q(u | u(k)) ≥ J (u) for all u.Minimizing the majorant leads to the normal equations: (3.3) (H⊤H + λΨ⊤(P(k) 

> ϵ

)2Ψ)u = H⊤b.

The MM principle guarantees that minimizing the majorant decreases the original objective, ensuring convergence to a stationary point under mild conditions. 

3.2 Generalized Krylov Subspace Projection. Solving (3.3) directly is im-practical for large-scale problems. MM-GKS instead projects onto a small subspace spanned by generalized Krylov vectors that capture information about both the data misfit and the regularization. Starting from an initial basis Vℓ ∈ Rn×ℓ with orthonor-mal columns (generated by Golub-Kahan bidiagonalization applied to H), we seek an approximate solution u(k+1) = Vℓz by computing QR factorizations (3.4) HV ℓ = QARA, P(k) 

> ϵ

ΨV ℓ = QΨRΨ,

and solving the reduced least-squares problem: (3.5) z(k+1) = arg min 

> z∈Rℓ

"

RA√λRΨ

#

z −

"

Q⊤ 

> A

b0

# 22

.

The regularization parameter λ is selected at each iteration using the discrepancy principle applied to the reduced problem (generalized cross validation (GCV) may be used alternatively when the noise level is unknown). If the residual of the nor-mal equations is not sufficiently small, the subspace is expanded by appending the normalized residual direction: (3.6) r(k+1) = H⊤(Hu (k+1) − b) + λΨ⊤(P(k) 

> ϵ

)2Ψu (k+1) ,NL-RMM-GKS FOR DYNAMIC AND STREAMING INVERSE PROBLEMS 7(3.7) Vℓ+1 = [ Vℓ, r(k+1) /∥r(k+1) ∥2].

This expansion is repeated, updating the weights P(k) 

> ϵ

at each iteration, until conver-gence. The full MM-GKS algorithm is given in Algorithm B.1 of the supplementary material. 

3.3 Recycling: RMM-GKS. For large-scale problems requiring many itera-tions, MM-GKS exhausts available memory as the subspace dimension grows without bound. The RMM-GKS algorithm [21] addresses this through an enlarge-compress cycle that keeps memory bounded while preserving the most informative search di-rections. 

Enlargement. Starting from a basis of dimension kmin , MM-GKS expansion steps are performed as described above, growing the subspace up to a maximum dimension 

kmax .

Compression. When the basis reaches kmax columns, it is compressed back to kmin 

columns by applying a compression function χ that computes the truncated SVD 1

of the stacked matrix (3.8) Hkmax := 

"

RA√λcurr RΨ

#

,

and retains the kmin − 1 right singular vectors corresponding to the largest singular values, forming W ∈ Rkmax ×(kmin −1) . The compressed basis is ˜V = Vkmax W. To guarantee monotonic decrease of the objective, the normalized component of the cur-rent solution orthogonal to ˜V is appended, forming the new kmin -dimensional basis. This enlarge-compress cycle repeats until convergence. The Enlarge and Compress subroutines are given as Algorithms B.2 and B.3 in the supplementary material; the full RMM-GKS algorithm is given as Algorithm 3.1 below. 

3.4 Streaming: s-RMM-GKS. The streaming variant of RMM-GKS [21] addresses scenarios where data arrives sequentially or memory constraints prevent processing the full dataset simultaneously. Given blocks {Hj , bj }Nj=1 as defined in Section 2.4, we process each block in sequence, initializing with the solution u(j−1) 

and compressed basis V(j−1)  

> kmin

carried over from the previous block. This allows re-construction from arbitrarily large datasets with constant memory requirements, as formalized in Algorithm 3.2. 

4 Nonlinear RMM-GKS Framework. We now extend RMM-GKS to han-dle forward operators H(p) that depend nonlinearly on unknown parameters p. We present two complementary realizations of the joint optimization problem (4.1) min 

> u,p

12 ∥H(p)u − b∥22 + λ∥Ψu ∥1 :1. Alternating Minimization (AltMin): Decouples the joint problem by alternating between image updates via RMM-GKS and parameter updates via Gauss-Newton steps. 2. Variable Projection (VarPro): Eliminates the image variable through an implicit function, reducing to optimization over parameters only.   

> 1Because Hkmax is relatively small, computing the tSVD is inexpensive.

8 OKUNOLA T., PASHA M., KILMER M., NAGY J. DE STURLER E. 

Algorithm 3.1 RMM-GKS (with optional fixed λ)

Require: H , Ψ, d, u(0) , V(0) , k min , k max , ϵ, tol 1, λ fix 

Ensure: (u(i+1) , Vkmin , λ (i)) 

> 1:

s = kmax − kmin  

> 2:

if V (0) is given  

> 3:

Vkmin = V(0)  

> 4:

else  

> 5:

Initialize basis Vkmin via Golub-Kahan bidiagonalization  

> 6:

end if  

> 7:

for i = 1 , 2, . . . , i max  

> 8:

(u(i+1) , λ (i+1) , Vkmax , R H , R Ψ) = Enlarge( H, Ψ, Vkmin +1 , d, u(i), ϵ, s, tol 1, λ fix ) 

> 9:

Vkmin = Compress( Vkmax , R H , R Ψ, d, u(i+1) , Q H , k min , λ (i+1) ) 

> 10:

Update weights P(i+1)  

> ϵ

from u(i+1)  

> 11:

r(i+1) = H⊤(Hu (i+1) − b) + λ(i+1) Ψ⊤(P(i+1)  

> ϵ

)2Ψu (i+1)  

> 12:

r(i+1) ← r(i+1) − Vkmin V⊤ 

> kmin

r(i+1)  

> 13:

Vkmin +1 ← [Vkmin , r(i+1) /∥r(i+1) ∥2] 

> 14:

end for  

> 15:

Output: (u(i+1) , Vkmin , λ (i+1) )

Algorithm 3.2 s-RMM-GKS 

Require: Blocks {(Hj , dj )}Nj=1 , regularizer Ψ, initial iterate u(0) , basis size (kmin , k max ), parameter ϵ, tolerance tol 1

Ensure: Approximate streaming solution u∗ 

> 1:

Initialize V(0)  

> kmin
> 2:

for j = 1 to N 

> 3:

(u(j), V(j) 

> kmin

) = RMM-GKS 



Hj , Ψ, dj , u(j−1) , V(j−1)  

> kmin

, k min , k max , ϵ, tol 1

 

> 4:

end for  

> 5:

u∗ = u(N )

Both approaches share the same inner MM-GKS machinery and differ only in how parameter updates are coupled to image reconstruction. Problem (4.1) is nonlinear in 

p and non-smooth in u; the former requires Gauss-Newton iteration while the latter is handled by the majorization-minimization approach of Section 3. 

4.1 Alternating Minimization (AltMin). Alternating minimization decou-ples the joint problem into two subproblems solved iteratively, fully optimizing each variable while holding the other fixed. 

4.1.1 Image Update. With the current parameter estimate p(k) fixed, we solve for the image by constructing the quadratic majorant and minimizing over a recycled Krylov subspace: (4.2) u(k+1) = arg min 

> u

12 ∥H(p(k))u − b∥22 + λ

2 ∥P(k) 

> ϵ

Ψu ∥22,

where P(k) 

> ϵ

is computed from u(k) as in Section 3. We apply the enlarge-compress cycle of RMM-GKS, projecting onto subspaces of dimension between kmin and kmax and selecting λ adaptively via the discrepancy principle on the reduced problem. Although NL-RMM-GKS FOR DYNAMIC AND STREAMING INVERSE PROBLEMS 9

H(p(k)) changes across outer iterations as parameters are updated, within each inner RMM-GKS solve the operator is fixed, so RMM-GKS applies without modification. 

4.1.2 Parameter Update. With the updated image u(k+1) fixed, we mini-mize the data fidelity term with respect to parameters using damped Gauss-Newton iteration with backtracking line search: (4.3) p(k+1) = arg min 

> p

12 ∥H(p)u(k+1) − b∥22.

At each inner iteration ℓ, the Gauss-Newton direction solves (4.4) ∆p(ℓ) = arg min  

> ∆p

∥J(ℓ)∆p + r(ℓ)∥22,

where J(ℓ) = ∂∂p (H(p)u(k+1) ) p=p(ℓ) and r(ℓ) = H(p(ℓ))u(k+1) − b. The step size 

α(ℓ) ∈ (0 , 1] is chosen via backtracking line search satisfying the Armijo condition, and inner iterations continue until ∥∆p(ℓ)∥ < tol p or ℓ = ℓp

> max

. The full parameter update procedure is given in Algorithm 4.1. 

Algorithm 4.1 UPDATE-PARAM (AltMin parameter update at outer iteration k)

Require: u (k), b, p(k−1) ∈ Rnp , maxiter p ≥ 1, damping μ > 0

Ensure: p (k) 

> 1:

Initialize: p(k−1,0) ← p(k−1)  

> 2:

for ℓ = 0 , . . . , maxiter p − 1 

> 3:

g(k,ℓ ) = ∇pJϵ,λ (u(k), p(k−1,ℓ )) 

> 4:

for i = 1 , . . . , n p 

> 5:

Hpi = ∂H(p(k−1,ℓ )) 

> ∂p i
> 6:

[Ju]:,i = Hpi u(k) 

> 7:

end for  

> 8:

J(k,ℓ ) 

> p

= J⊤ 

> u

Ju + μI 

> 9:

Solve J(k,ℓ ) 

> p

d(k,ℓ ) = −g(k,ℓ ) 

> 10:

Find α(k,ℓ ) satisfying Armijo condition  

> 11:

p(k−1,ℓ +1) = p(k−1,ℓ ) + α(k,ℓ ) d(k,ℓ ) 

> 12:

end for  

> 13:

p(k) ← p(k−1,maxiter p)

4.2 Variable Projection (VarPro). The variable projection approach elim-inates u by solving the majorized image subproblem implicitly, reducing the joint problem to optimization over p alone. 

Majorization and Closed-Form Solution. Given an iterate u(k), we form weights 

P(k) = diag((( Ψu (k))2 

> j

+ ϵ2)−1/4) and define the weight operator ˆΨ(k)

= P(k)Ψ. For fixed p and P(k), the majorized subproblem (4.5) min 

> u

12 ∥H(p)u − b∥22 + λ

2 ∥ ˆΨ(k)

u∥22

has the closed-form solution (4.6) u(p) = 



H(p)⊤H(p) + λ( ˆΨ(k)

)⊤ ˆΨ(k)

−1

H(p)⊤b.10 OKUNOLA T., PASHA M., KILMER M., NAGY J. DE STURLER E. 

Reduced Objective and Gauss-Newton Update. Substituting (4.6) into the ob-jective defines the reduced function f (p) = 12 ∥r(p)∥22 + λ 

> 2

∥s(p)∥22, where r(p) = 

H(p)u(p) − b and s(p) = ˆΨ(k)

u(p). The Gauss-Newton update solves (4.7) 



J⊤ 

> r

Jr + λ J⊤ 

> s

Js



∆p = −



J⊤ 

> r

r + λ J⊤ 

> s

s



,

where the Jacobian columns [ Jr](: ,j ) and [ Js](: ,j ) are computed via the sensitivity equation (4.8) Mλ(p) upj = −



Hpj (p)⊤r(p) + H(p)⊤Hpj (p) u(p)



,

with Mλ(p) = H(p)⊤H(p) + λ( ˆΨ(k)

)⊤ ˆΨ(k)

and Hpj = ∂H/∂p j . In practice the inner subproblem (4.5) is solved inexactly via RMM-GKS, and the weights P(k) are updated each outer iteration. The VarPro parameter update procedure is given in Algorithm B.4 of the supplementary material. 

4.3 High-Regularization Stabilization. For both AltMin and VarPro, when the adaptively chosen λ(k) is small, the image update u(k+1) may be insufficiently regularized, causing the subsequent parameter update to be attracted to poor local minima. To mitigate this, we optionally perform a second RMM-GKS solve with a significantly larger regularization parameter λhi ≫ λ(k), producing a smoother image estimate ˆ u(k+1) that is used only for the parameter update. The primary solution 

u(k+1) (obtained with the adaptively chosen λ(k)) is retained as the image recon-struction output and its associated compressed basis V(k+1)  

> kmin

is carried forward to the next outer iteration. The high-regularization solve uses the basis output of the primary solve as its starting point but produces a separate compressed basis that is discarded after the parameter update. This stabilization strategy is summarized in Algorithm 4.2. 

4.4 Convergence Analysis. The following assumptions are standard in non-linear optimization and hold for common imaging operators such as CT with angular perturbations and PAT with radial shifts. 

Assumption 4.1 (Regularity conditions). (i) The forward operator H(p) is twice continuously differentiable with bounded derivatives: ∥∂H/∂p i∥2 ≤ CH and 

∥∂2H/∂p i∂p j ∥2 ≤ CHH . (ii)The iterates are bounded: ∥u(k)∥2 ≤ Ru and ∥p(k)∥2 ≤

Rp. (iii) The damped Gauss-Newton matrix satisfies γmin I ⪯ J(k,ℓ ) 

> p

⪯ γmax I for constants 0 < γ min ≤ γmax < ∞, where γmin ≥ μ is ensured by the damping parameter. 

Theorem 4.2 (Convergence to stationary point). Let {(u(k), p(k))} be the se-quence generated by Algorithm 4.2 under Assumption 4.1. Then every limit point 

(u∗, p∗) is a stationary point of (4.1) , satisfying 

(4.9) ∇uJϵ,λ (u∗, p∗) = 0 and ∇pJϵ,λ (u∗, p∗) = 0.

Remark 4.3. The proof establishes convergence by showing that each outer itera-tion produces sufficient descent in Jϵ,λ : the image update decreases the objective by at least ∥∇ uJ ∥ 22/(2¯ μ), where ¯ μ = C2 

> H

+ λε −1C2 

> Ψ

bounds the majorant Hessian, while the Gauss-Newton parameter update with Armijo line search decreases it by at least 

c1αmin γ−1max ∥∇ pJ ∥ 22. Since the objective is bounded below, both gradient norms are summable and hence converge to zero. The complete proof, including all technical lemmas and the extension to multiple inner iterations, is given in Appendix A. NL-RMM-GKS FOR DYNAMIC AND STREAMING INVERSE PROBLEMS 11 

Algorithm 4.2 NL-RMM-GKS 

Require: b , Ψ, p(0) , k min , k max , ϵ 

Ensure: (u∗, p∗) 

> 1:

Initialize V(0)  

> kmin
> 2:

for k = 0 , 1, 2, . . .  

> 3:

Hk = H(p(k)) 

> 4:

Primary image solve: 

(u(k+1) , λ (k), V(k+1)  

> kmin

) = RMM-GKS( Hk, Ψ, b, u(k), V(k) 

> kmin

, k min , k max , ϵ ) 

> 5:

Optional high-regularization solve: 

(ˆ u(k+1) , ˆλ(k), ˆVkmin ) = RMM-GKS( Hk, Ψ, b, u(k+1) , V(k+1)  

> kmin

, k min , k max , ϵ ; λhi )// ˆVkmin is discarded after this step  

> 6:

Parameter update: p(k+1) = UPDATE-PARAM( Hk, Ψ, ˆu(k+1) , b, p(k), ˆλ(k)) 

> 7:

Update weights: P (k+1)  

> ϵ

from u(k+1)  

> 8:

Compute and project residual:  

> 9:

r(k+1) = H⊤ 

> k

(Hku(k+1) − b) + λ(k)Ψ⊤(P(k+1)  

> ϵ

)2Ψu (k+1)  

> 10:

r(k+1) ← r(k+1) − V(k+1) 

> kmin



V(k+1) 

> kmin

⊤

r(k+1)  

> 11:

Update search space:  

> 12:

V(k+1)  

> kmin

← [V(k+1)  

> kmin

, r(k+1) /∥r(k+1) ∥2] 

> 13:

Check outer convergence  

> 14:

end for  

> 15:

u∗ = u(k+1) , p∗ = p(k+1) 

Remark 4.4. Theorem 4.2 provides a first-order necessary condition for optimal-ity. Global convergence to the global minimum cannot be guaranteed for nonconvex problems; however, our experiments in Section 6 demonstrate that the method re-liably recovers ground truth parameters when initialized via the coarse grid search described in Section 6. 

Remark 4.5 (Relationship to prior work). When p is known and fixed, Algo-rithm 4.2 reduces exactly to RMM-GKS [21] applied to the linear problem (1.3). The dynamic extension presented in Section 7 further reduces to MMGKS-OF [20] (with optical flow) or the framework of [23] (with ANISO-TV) when p is known. NL-RMM-GKS therefore strictly generalizes all of these methods. 

Remark 4.6 (Scope of convergence guarantee). Theorem 4.2 and its proof (Ap-pendix A) assume that the parameter update operates on the same image u(k+1) 

produced by the primary RMM-GKS solve. When the optional high-regularization stabilization (Section 4.3) is active, the parameter update instead uses the smoothed estimate ˆu(k+1) , and the descent chain requires an additional argument relating 

J (ˆ u(k+1) , p(k+1) ) to J (u(k+1) , p(k+1) ). The convergence guarantee therefore applies directly to Algorithm 4.2 when the high-regularization step is omitted; extending it formally to the stabilized variant is left to future work. 12 OKUNOLA T., PASHA M., KILMER M., NAGY J. DE STURLER E. 

5 Streaming Extensions. We now extend both AltMin and VarPro real-izations of NL-RMM-GKS to handle streaming data, where measurement blocks 

{(Hj (·), bj )}Nj=1 arrive sequentially or cannot all be held in memory simultaneously (see Section 2.4 for the block partitioning setup). The key idea is to carry over three quantities between blocks: the current image estimate u(j), the current parameter estimate p(j), and the compressed Krylov basis V(j) 

> kmin

. Carrying over the basis is what distinguishes streaming NL-RMM-GKS from simply running NL-RMM-GKS independently on each block: the basis encodes solution structure learned from all previously seen data, allowing each new block to build on rather than discard prior information. Multiple passes over the full dataset are performed until the outer con-vergence criterion is met. When p is known and fixed, s-NL-RMM-GKS reduces exactly to s-RMM-GKS [21], confirming that our framework is a strict generalization of the linear streaming method. 

5.1 Algorithm. At each pass, blocks j = 1 , . . . , N are processed in sequence. For block j, we run NL-RMM-GKS (Algorithm 4.2) using only the data ( Hj (·), bj ), initialized with the solution triple ( u(j−1) , p(j−1) , V(j−1)  

> kmin

) carried over from the previ-ous block. The outputs ( u(j), p(j), V(j) 

> kmin

) are then passed to block j +1. At the end of each pass, convergence is checked on the full dataset; if not converged, the next pass begins with the final block’s outputs used to initialize block 1. The full procedure is given in Algorithm 5.1. 

5.2 Memory and Complexity Analysis. A key advantage of the streaming approach is that memory requirements are bounded independently of the total dataset size. We summarize the dominant memory costs below. 

Basis storage. At any point in the algorithm, only the current compressed basis 

V(j) 

> kmin

∈ Rn×kmin and the enlarged basis V(j) 

> kmax

∈ Rn×kmax need to be stored. Since 

kmin and kmax are fixed hyperparameters, basis storage costs O(nk max ) regardless of the number of blocks N or the number of passes. By contrast, standard MM-GKS without recycling requires O(nK ) storage where K is the total iteration count, which grows unboundedly. 

Forward operator storage. Only the current block Hj (p(j−1) ) ∈ Rmj ×n needs to be formed and stored at any one time, costing O(mj n) where mj = m/N for equal-sized blocks. The full operator H(p) ∈ Rm×n is never formed explicitly, giving an 

O(N ) reduction in operator storage compared to the non-streaming case. 

Parameter and image storage. The image u(j) ∈ Rn and parameter vector p(j) ∈

Rnp are carried between blocks at cost O(n + np), which is independent of N .

Total memory. The dominant cost is basis storage O(nk max ), which is fixed by the recycling window size and independent of N , the number of passes, and the total number of measurements m. In practice, the O(N ) reduction in operator storage is the more significant saving for large-scale problems, as m ≫ n is common in tomo-graphic applications. The trade-off between N and reconstruction quality is explored empirically in Section 6. 

Per-pass complexity. Each pass over all N blocks requires N ·O (kmax −kmin ) inner RMM-GKS iterations, each costing O(mj n) for the matrix-vector products with Hj .The Gauss-Newton parameter update costs O(n2

> p

mj ) per block for forming and solving the reduced system. Since np ≪ mj in our applications (we estimate a small number of geometric parameters), this cost is negligible relative to the image solve. 

6 Numerical Results. We demonstrate the effectiveness of NL-RMM-GKS on computed tomography and photoacoustic tomography problems with geometric NL-RMM-GKS FOR DYNAMIC AND STREAMING INVERSE PROBLEMS 13 

Algorithm 5.1 s-NL-RMM-GKS (Streaming Nonlinear RMM-GKS) 

Require: {(Hj (·), bj )}Nj=1 , Ψ, ( u(0) , p(0) ), kmin , k max , ϵ, λ fix , λ hi , tol outer 

Ensure: (u∗, p∗) 

> 1:

Initialize V(0)  

> kmin
> 2:

for pass = 1 , 2, . . . until outer convergence  

> 3:

for j = 1 to N 

> 4:

Hj = Hj (p(j−1) ) 

> 5:

Primary image solve: 

(u(j), V(j) 

> kmin

, λ (j)) = RMM −GKS( Hj , Ψ, bj , u(j−1) , V(j−1)  

> kmin

, k min , k max , ϵ, tol u) 

> 6:

Optional high-regularization solve: 

(ˆ u(j), ˆVkmin , ˆλ(j)) = RMM −GKS( Hj , Ψ, bj , u(j), V(j) 

> kmin

, k min , k max , ϵ, tol u, λ hi )// ˆVkmin discarded after this step  

> 7:

Parameter update: p(j) = UPDATE −PARAM( Hj , Ψ, ˆu(j), bj , p(j−1) , ˆλ(j)) 

> 8:

end for  

> 9:

Check outer convergence on full dataset: 

∥u(N ) − u(N )prev ∥∥u(N )prev ∥ < tol outer and ∥∆p(N )∥ < tol p 

> 10:

end for  

> 11:

u∗ = u(N ), p∗ = p(N )

uncertainty. All CT experiments are simulated using the TRIPS-Py package [22] and the ASTRA toolbox [27], while PAT problems are simulated using a Python transla-tion of the IR-Tools package [11]. Our experiments address the following questions: 1. Memory efficiency: Does recycling match the convergence of standard MM-GKS while maintaining bounded memory? 2. Streaming performance: Can streaming variants (s-NL-RMM-GKS) achieve near-full-data quality, and how does performance vary with the number of blocks N ?3. Dynamic reconstruction: For dynamic phantoms, how do temporal reg-ularization strategies (ANISO-TV vs. optical flow) perform when combined with geometry estimation? 4. Recycling window size: How does kmax affect the trade-off between mem-ory, convergence speed, and reconstruction quality? The AltMin vs. VarPro robustness comparison is addressed in the supplemen-tary material (Section S4), where we show that VarPro converges faster with good initialization while AltMin is more robust to poor initialization. 

6.1 Experimental Setup. 

Quality metrics. We evaluate reconstruction quality using the relative reconstruc-tion error (RRE) ∥u(k) − utrue ∥2/∥utrue ∥2 and the relative parameter error ∥p(k) −14 OKUNOLA T., PASHA M., KILMER M., NAGY J. DE STURLER E. 

ptrue ∥2/∥ptrue ∥2.

Stopping criteria. We terminate when the relative solution change satisfies ∥u(k+1) −

u(k)∥/∥u(k)∥ < 10 −3, the parameter increment satisfies ∥∆p(k)∥ < 10 −4, or the max-imum iteration count is reached. 

Common parameters. Unless otherwise stated, all experiments use recycling win-dow kmin = 5, kmax = 25, truncated SVD compression retaining kmin − 1 singular vectors, and regularization parameter selected by the discrepancy principle. All ex-periments are run on a Dell XPS 16 with single-threaded execution. 

Parameter initialization. We initialize p(0) using a coarse grid search in a low-dimensional subspace: an initial basis Vℓ0 with ℓ0 = 10 is generated via Golub-Kahan bidiagonalization at the nominal geometry, and for each candidate pcand on a coarse grid we solve the reduced problem with H(pcand )Vℓ0 and select the candidate yielding the smallest data residual. This is computationally cheap since only the reduced subspace problem is solved. 

6.2 Computed Tomography with Uncertain Projection Angles. In fan-beam CT, the forward operator H(p) implements the Radon transform at perturbed angles θ(p) = [ θnom 1 + p, . . . , θ nom  

> nθ

+ p]⊤, where the scalar p represents an unknown global angular shift. Even small errors of ±1–2 ◦ can produce significant reconstruction artifacts [19], motivating joint estimation of p alongside the attenuation image. 

6.2.1 Test 1: Static Shepp-Logan Reconstruction. 

Setup. We use a 256 ×256 Shepp-Logan phantom with nθ = 180 projection angles randomly selected from [0 ◦, 180 ◦) and nr = 362 detector bins. Geometry uncertainty is introduced by a random angular shift ptrue = 0 .2421 ◦, with 1% Gaussian noise added to the sinogram. We initialize at p(0) = 0 ◦ via grid search. For streaming experiments, the 180 angles are partitioned into N randomly assigned blocks of approximately equal size ( N = 2 , 4, 6, 10). 

Goal. Our first goal is to compare the computational efficiency, memory usage, and convergence behavior of standard MM-GKS without recycling (memory grows lin-early with iterations), NL-RMM-GKS ( N = 1, full data with recycling), and streaming s-NL-RMM-GKS with varying block counts ( N = 2 , 4, 6, 10). Additionally, in Sec-tion B.2, we investigate how the number of inner solver iterations affects streaming performance when N = 4. 

Results. Figure 6.1 shows that all methods successfully recover the true angle (relative parameter error < 10 −3), but reconstruction quality differs substantially. Standard MM-GKS without recycling stagnates at RRE = 0 .1805, while NL-RMM-GKS ( N = 1) converges to RRE = 0 .0790—a 56% improvement—demonstrating that recycling improves convergence quality, not just memory efficiency. Increasing 

N trades quality for speed (Figure 6.2): at N = 10 we achieve a 5.2 × speedup over 

N = 2 with peak memory reduced by 8%, at the cost of RRE increasing to 0.1872. Visual results (Figure 6.3) confirm that N ≤ 4 maintains good edge preservation. Table 6.1 summarizes all results. The effect of inner iteration count on streaming performance is studied in the supplementary material (Section S2), where we show that 10 inner iterations provides a good speed-accuracy balance. 

6.3 Photoacoustic Tomography with Sensor Radius Uncertainty. In our circular array PAT setup, nc concentric circles of sensors surround the imaging region with nominal radii rnom  

> j

= 2 j/n c. A global radial shift p affects all circles identically: rj (p) = rnom  

> j

+ p. This uniform perturbation arises physically from sound speed variations in the coupling medium, sensor positioning errors, or systematic NL-RMM-GKS FOR DYNAMIC AND STREAMING INVERSE PROBLEMS 15                  

> Fig. 6.1: Test 1 convergence comparison. (a) RRE vs. outer iteration: NL-RMM-GKS outperforms standard MM-GKS, with streaming variants trading quality for speed. (b) Parameter error converges rapidly for all methods, reaching <10 −3by iteration 5.
> Fig. 6.2: Test 1 streaming performance vs. number of blocks. (a) Final RRE increases with
> N.(b) Runtime decreases substantially (5.2 ×speedup from N= 2 to N= 10). (c) Peak memory drops 8% at N= 10.

acquisition delays, and motivates joint estimation of p alongside the absorption image. 

6.3.1 Test 2: Static Tectonic Phantom—Initialization Sensitivity 

Setup. We use a 64 × 64 tectonic plate phantom featuring sharp boundaries be-tween regions of different absorption. This phantom is specifically designed to test edge preservation under geometric uncertainty. We introduce a radius perturbation 

ptrue = 0 .432 units applied uniformly to all circles. To assess robustness to initialization quality, we test four scenarios with increasing initial error:  

> ■

Very good : Initial offset = 0 .1 units from truth  

> ■

Good : Initial offset = 0 .2 units from truth  

> ■

Poor : Initial offset = 0 .5 units from truth  

> ■

Very poor : Initial offset = 0 .6 units from truth We use streaming with N = 3 blocks and standard recycling parameters ( kmin = 5, 

kmax = 25). 

Goal. Investigate the robustness of AltMin vs. VarPro implementations to poor parameter initialization. 

Results. Figure 6.4 reveals a clear pattern: when initialization is good (offsets 0.1, 0.2), both VarPro (solid lines) and AltMin (dashed lines) converge smoothly and achieve similar final reconstruction quality. VarPro actually converges slightly faster 16 OKUNOLA T., PASHA M., KILMER M., NAGY J. DE STURLER E.                                                       

> Fig. 6.3: Test 1 visual comparison. Shepp-Logan reconstructions for N= 1 ,2,6,10 blocks compared to standard MM-GKS. Progressive blurring of ellipse boundaries is visible as N
> increases, particularly in small interior structures. Table 6.1: Test 1: Static Shepp-Logan CT with angle uncertainty ( ptrue = 0 .2421 ◦,p(0) =0◦). Method Time Peak Mem. Final Param. p∗
> (min) (GB) RRE Err. (◦)MM-GKS (no recycling) 2.08 0.220 0.1805 1.49e-4 0.2421 NL-RMM-GKS ( N= 1) 1.87 0.217 0.0790 7.36e-4 0.2419 s-NL-RMM-GKS ( N= 2) 1.19 0.198 0.0948 2.89e-4 0.2422 s-NL-RMM-GKS ( N= 4) 0.82 0.188 0.1453 5.35e-3 0.2408 s-NL-RMM-GKS ( N= 6) 0.80 0.185 0.1627 1.28e-2 0.2390 s-NL-RMM-GKS ( N= 10) 0.40 0.182 0.1872 1.87e-2 0.2376

in these cases, reaching low RRE by iteration 10. However, the story changes for poor initialization. At offset = 0 .5 (green curves), we start to see VarPro exhibit more oscillatory behavior in panel (a), while AltMin maintains steadier convergence. At the worst initialization (offset = 0 .6), VarPro struggles significantly—its reconstruction error oscillates wildly between 2 and 7 be-fore slowly improving, and it never fully stabilizes even by iteration 20. In contrast, AltMin with offset = 0 .6 shows some initial oscillations but settles into monotonic convergence after a few iterations. This difference in robustness makes sense from an algorithmic perspective: VarPro eliminates the image variable u analytically, creating a reduced but highly nonlinear problem in p alone. When far from the solution, this nonlinearity can cause the optimization landscape to be rough with many local irregularities. AltMin, by alter-nating between u and p updates, effectively smooths out this landscape at the cost of requiring more outer iterations. Panel (b) shows that parameter estimation follows similar trends, with VarPro exhibiting larger oscillations for poor initialization. The visual reconstructions in Figure 6.5 confirm that both methods eventually produce comparable image quality, but AltMin gets there more reliably when starting far from the truth. We recommend using VarPro when you have a decent initial guess (e.g., from cal-ibration or grid search), but preferring AltMin when initialization quality is uncertain or the problem is particularly challenging. 

7 Extension of NL-RMM-GKS to Dynamic Problems. We now turn to the dynamic setting, where the imaging target changes across nt time frames and the NL-RMM-GKS FOR DYNAMIC AND STREAMING INVERSE PROBLEMS 17 

Fig. 6.4: Test 2. initialization sensitivity comparison. (a) RRE vs. iteration for both VarPro (solid) and AltMin (dashed) across four initialization qualities. Poor initialization (offsets 0.5, 0.6) causes VarPro to oscillate significantly, while AltMin maintains more stable convergence. (b) Parameter error shows similar trends, with VarPro more sensitive to initialization quality. 

Fig. 6.5: Test 2. visual comparison of final reconstructions for different initialization offsets. Columns show ground truth, VarPro, and AltMin results across δ ∈ { 0.05 , 0.30 }. Despite con-vergence differences, both methods achieve similar final visual quality, with AltMin slightly better at preserving sharp boundaries for poor initialization. 

forward operator may depend on unknown geometric parameters p. The goal is to jointly reconstruct the full image sequence u = vec([ u(1) , . . . , u(nt)]) ∈ Rn, with n =

nsnt, and to estimate p, from noisy measurements b. This section is self-contained: we introduce the dynamic forward model, present the full mathematical descriptions of both temporal regularization strategies (ANISO-TV and optical flow), give dedicated algorithm descriptions, and report numerical experiments on two dynamic phantoms. 

7.1 Dynamic Forward Model and Problem Formulation In a dynamic inverse problem the forward operator is block-diagonal, (7.1) H(p) = blockdiag  H1(p), . . . , Hnt (p) ∈ Rm×n,

where Ht(p) ∈ Rmt×ns is the forward operator at time t and m = P 

> t

mt. When the geometry is the same at every frame, Ht(p) = H(p) for all t and the block-diagonal structure reduces to Int ⊗ H(p). The joint reconstruction problem takes the form (7.2) min 

> u,p

12 ∥H(p)u − b∥22 + λ∥Θ(u) u∥1,

where Θ(u) is a combined spatio-temporal regularization operator whose form de-pends on the chosen temporal strategy, as described in Sections 7.2.1 and 7.2.2 below. 18 OKUNOLA T., PASHA M., KILMER M., NAGY J. DE STURLER E. 

7.2 Temporal Regularization Strategies 7.2.1 Anisotropic Space-Time Total Variation (ANISO-TV). Follow-ing [21], we promote sparse spatial gradients within each frame and penalize frame-to-frame intensity differences simultaneously. Let Ls =

Inh ⊗ Lv

Lh ⊗ Inv



be the spatial finite-difference operator, where Lv and Lh discretize the first derivative in the verti-cal and horizontal directions respectively. The ANISO-TV regularization operator is (7.3) D1 =

Int ⊗ Ls

Lt ⊗ Ins



,

where Lt is the temporal finite-difference operator. The regularization term ∥D1u∥1

penalizes both spatial gradients within each frame and differences between consecutive frames. Setting Θ = D1 (constant, independent of u) in (7.2) recovers the AnisoTV formulation of [21]. ANISO-TV is well suited to slowly varying sequences but provides no motion model and degrades when inter-frame displacements are large. 

7.2.2 Optical Flow Regularization (OF). When the dynamic sequence ex-hibits coherent motion, we incorporate a physics-based temporal model following [20]. The optical flow constraint (OFC) assumes that pixel intensities are preserved as ob-jects move: for a pixel at location ( xi, y i) with velocity si(t) = ( six(t), s iy (t)), (7.4) uix(t) six(t) + uiy (t) siy (t) + uit(t) = 0 ,

where uix, u iy , u it denote partial derivatives of the image with respect to x, y, and t.Assembling this across all pixels and time pairs gives the system Υ(u) s + ut = 0,where Υ(u) = diag( Υ(u(1) ), . . . , Υ(u(nt−1) )) is a block-diagonal matrix of spatial image gradients, and ut encodes temporal differences. 

Velocity estimation. Given the current image estimate u(k), the forward velocity field s(k) is estimated by solving the regularized least-squares problem (7.5) s(k) = arg min 

> s

Υ(u(k))s + u(k)

> t
> pp

+ γ∥ˆLs ∥qq ,

where ˆL is a discrete gradient operator that regularizes the velocity field, and p, q ∈{1, 2}. The reverse velocity field s′(k) is approximated via the relation ( s′ 

> x

+sx(t), s ′ 

> y

+

sy (t)) ≈ − (sx(t), s y (t)) to halve computation [20]. Each per-frame subproblem is solved efficiently with MMGKS using automatic regularization parameter selection via GCV. 

Motion operator encoding. Given s(k) and s′(k), we construct the block-structured motion matrices (7.6) ¯M(s(k)) = 



I −M(s(k)(1)) . . . . . .

I −M(s(k)(nt − 1)) 

 ,

and ¯M′(s′(k)) analogously for the reverse flow, where each M(s(k)(t)) ∈ Rns×ns is a bilinear warping operator encoding per-frame displacements [20]. The combined motion regularization operator is (7.7) ˆM(s(k), s′(k)) = 

" ¯M(s(k))¯M′(s′(k))⊤

#

.NL-RMM-GKS FOR DYNAMIC AND STREAMING INVERSE PROBLEMS 19 

Image reconstruction step. With fixed velocity estimates, we set the combined regularization operator (7.8) Θ(k) =

"

Ψ

ˆM(s(k), s′(k))

#

,

where Ψ is the spatial regularization operator. Problem (7.2) then becomes (7.9) u(k+1) = arg min 

> u

12 ∥H(p(k))u − b∥22 + λ∥Θ(k)u∥1,

which is solved via RMM-GKS with the discrepancy principle. Unlike ANISO-TV, 

Θ(k) is updated at each outer iteration as the image estimate improves, so the regu-larizer adapts to the evolving motion field. 

7.3 Dynamic NL-RMM-GKS Algorithm Both ANISO-TV and OF plug directly into the NL-RMM-GKS framework (Algorithm 4.2) by supplying the appro-priate operator Θ(k) at each outer iteration. The key difference from the static case is the additional velocity estimation step required by OF before each image solve. Algo-rithm 7.1 states the unified dynamic procedure; setting Θ(k) = D1 (constant) recovers the ANISO-TV variant, while executing the Solve-OF subroutine and forming Θ(k)

via (7.8) gives the OF variant. 

Streaming extension. The streaming variant of the dynamic algorithm (s-NL-RMM-GKS, dynamic) follows Algorithm 5.1 verbatim, with Θ(k) replaced by the dynamic operator at each block. Velocity estimation for OF is performed once per outer block using the current image estimate before the primary image solve; the estimated velocity field is then held fixed within that block. When p is known and fixed, and ANISO-TV is used, this reduces exactly to s-RMM-GKS applied to a dynamic linear problem [21]. 

Relationship to prior work. When p is known, Algorithm 7.1 with ANISO-TV reduces to the dynamic MM-GKS framework of [21], and with OF reduces to MMGKS-OF [20]. NL-RMM-GKS therefore strictly generalizes both methods to the case of uncertain forward operators. 

7.4 Experimental Setup All quality metrics, stopping criteria, and common algorithmic parameters are as described in Section 6. 

7.5 Test 3: Dynamic Moving MNIST Sequence (CT) 

Setup. We use the Moving MNIST dataset [25], consisting of nt = 20 frames of size 64 × 64 pixels showing two smoothly translating digits. For each frame we acquire nθ = 10 randomly assigned fan-beam CT projections from [0 ◦, 180 ◦) with 

nr = 91 detector bins, representing a limited-angle sparse acquisition. A common angular perturbation ptrue = −0.1480 ◦ is applied to all frames, with initialization 

p(0) = −0.25 ◦. For streaming experiments, the 10 angles per frame are partitioned into N randomly assigned blocks of approximately equal size. 

Goal. We compare the two temporal regularization strategies—optical flow (OF) [20] and anisotropic total variation (ANISO-TV) [21]—within the NL-RMM-GKS frame-work, and investigate how streaming block count N affects each strategy. 

Results. Figure 7.1 reveals a striking difference between strategies. Optical flow consistently achieves lower reconstruction error across all N : with N = 1, OF con-verges to RRE ≈ 0.07 while ANISO-TV reaches only ≈ 0.30. This gap widens with 

N ; at N = 10, OF maintains RRE = 0 .41 while ANISO-TV stalls at RRE = 0 .67. 20 OKUNOLA T., PASHA M., KILMER M., NAGY J. DE STURLER E. 

Algorithm 7.1 Dynamic NL-RMM-GKS (with plug-in temporal regularization) 

Require: b , Ψ, p(0) , k min , k max , ϵ, λ hi , τ // τ : OF update frequency (set τ = 1 for every iteration) 

Ensure: (u∗, p∗) 

> 1:

Initialize u(0) , V(0)  

> kmin

, Θ(0) ← D1 // same initialization for both strategies  

> 2:

for k = 0 , 1, 2, . . .  

> 3:

Hk ← H(p(k)) 

> 4:

if OF variant and k mod τ = 0  

> 5:

Velocity estimation: for t = 1 , . . . , n t − 1: 

s(k)(t) = SOLVE-OF 



u(k)(t), u(k)(t+1) , ˆL

 

> 6:

Approximate s′(k) from s(k) via OFC relation (Remark 3.1 of [20])  

> 7:

Θ(k) ←

"

Ψ

ˆM(s(k), s′(k))

# 

> 8:

else if ANISO-TV variant  

> 9:

Θ(k) ← D1 // fixed; no motion estimation needed  

> 10:

end if  

> 11:

Primary image solve: 

(u(k+1) , λ (k), V(k+1)  

> kmin

) = RMM-GKS( Hk, Θ(k), b, u(k), V(k) 

> kmin

, k min , k max , ϵ ) 

> 12:

Optional high-regularization solve: 

(ˆ u(k+1) , ˆλ(k), ˆVkmin ) = RMM-GKS( Hk, Θ(k), b, u(k+1) , V(k+1)  

> kmin

, k min , k max , ϵ ; λhi )// ˆVkmin discarded after this step  

> 13:

Parameter update: p(k+1) = UPDATE-PARAM( Hk, Θ(k), ˆu(k+1) , b, p(k), ˆλ(k)) 

> 14:

Update weights P (k+1)  

> ϵ

from u(k+1)  

> 15:

Compute and project residual:  

> 16:

r(k+1) ← H⊤ 

> k

(Hku(k+1) − b) + λ(k)(Θ(k))⊤(P(k+1)  

> ϵ

)2Θ(k)u(k+1)  

> 17:

r(k+1) ← r(k+1) − V(k+1)  

> kmin

(V(k+1)  

> kmin

)⊤r(k+1)  

> 18:

V(k+1)  

> kmin

← [V(k+1)  

> kmin

, r(k+1) /∥r(k+1) ∥2] 

> 19:

Check outer convergence  

> 20:

end for  

> 21:

u∗ = u(k+1) , p∗ = p(k+1) 

The superiority of optical flow is expected here: the Moving MNIST digits undergo smooth translation, exactly the motion structure that OF is designed to capture, whereas ANISO-TV enforces temporal smoothness without any motion model and is better suited to sequences with small inter-frame changes. Parameter estimation (Figure 7.1(b)) is more robust than reconstruction to the choice of regularization, though ANISO-TV becomes unstable at large N . Visual results (Figure 7.2) confirm that OF preserves digit shapes clearly even at N = 10, while ANISO-TV produces NL-RMM-GKS FOR DYNAMIC AND STREAMING INVERSE PROBLEMS 21 increasingly unrecognizable digits. Full results are summarized in Table 7.1. 

Table 7.1: Test 3. Dynamic CT for Moving MNIST ( nt = 20 frames of 64 × 64, 10 angles per frame, ptrue = −0.1480 ◦, p(0) = −0.25 ◦). Optical flow consistently outperforms ANISO-TV, especially for larger N .

Method Final RRE Param. Err. p∗ (◦)

Optical Flow Regularization 

NL-RMM-GKS-OF ( N = 1) 0.0719 0.0016 -0.1477 s-NL-RMM-GKS-OF ( N = 2) 0.0979 0.0051 -0.1487 s-NL-RMM-GKS-OF ( N = 5) 0.2089 0.0538 -0.1400 s-NL-RMM-GKS-OF ( N = 10) 0.4071 0.0121 -0.1460 

Anisotropic TV Regularization 

NL-RMM-GKS-ANISO ( N = 1) 0.3040 0.1074 -0.1321 s-NL-RMM-GKS-ANISO ( N = 2) 0.3605 0.0838 -0.1355 s-NL-RMM-GKS-ANISO ( N = 5) 0.5030 0.1795 -0.1214 s-NL-RMM-GKS-ANISO ( N = 10) 0.6712 0.4634 -0.2165 

Fig. 7.1: Test 3. Convergence comparison across block counts and regularization strategies. (a) RRE vs. iteration: optical flow (solid) maintains stable convergence across all N , while ANISO-TV (dashed) degrades significantly at N = 10. (b) Parameter error: both methods converge initially, but ANISO-TV becomes unstable at large N .

7.6 Test 4: Dynamic Blocks Phantom (PAT) 

Setup. We consider nt = 10 frames of size 50 × 50 pixels containing four rectan-gular blocks of smoothly varying intensity, imaged with a circular array PAT sensor. A global radial shift ptrue = 0 .1924 units is introduced with initialization p(0) = 0 .25. We evaluate performance under recycling window sizes kmax ∈ { 10 , 15 , 20 , 25 } with 

kmin = 5 fixed, comparing OF and ANISO-TV regularization. A baseline that uses only the coarse grid-search parameter estimate and then solves the linear problem is included to demonstrate the benefit of joint estimation. 

Goal. To determine the optimal recycling window size for dynamic PAT problems, confirm the superiority of OF regularization in this setting, and demonstrate the benefit of full joint estimation over the grid-search baseline. 22 OKUNOLA T., PASHA M., KILMER M., NAGY J. DE STURLER E.           

> Fig. 7.2: Test 3. Visual comparison at three time points ( t= 0 ,10 ,19) for N∈ { 1,10 }.Optical flow preserves digit shapes significantly better than ANISO-TV, especially at N= 10 where ANISO-TV fails completely.

Results. Figure 7.3(a) shows reconstruction error decreasing steadily for all re-cycling window sizes, with curves tightly clustered, indicating that reconstruction quality is relatively insensitive to the window size within the tested range. Opti-cal flow consistently outperforms ANISO-TV by roughly 2 × in final RRE (0 .11–0 .13 vs. 0 .26–0 .28). Panel (b) shows that parameter estimation is even less sensitive to 

kmax : all configurations converge to parameter errors below 0 .01 by iteration 20. As shown in Table 7.2, increasing kmax from 10 to 25 reduces RRE from 0 .1322 to 0 .1113 for OF—a modest 16% improvement that must be weighed against the proportional memory increase, suggesting kmax ∈ [15 , 20] as a practical sweet spot. Visual results (Figure 7.4) confirm that OF preserves block edges well across all kmax values, while ANISO-TV produces noticeably blurred boundaries. Critically, the grid-search-only baseline produces substantially worse reconstructions than joint estimation under ei-ther regularization strategy, demonstrating that iterative parameter refinement is es-sential. 

8 Conclusions and Outlook. We have developed a comprehensive frame-work for nonlinear inverse problems with uncertain forward operators by extending the recycled majorization-minimization generalized Krylov subspace method to the nonlinear setting. The framework addresses the fundamental challenge that arises when the forward operator depends on unknown geometric or calibration parame-ters, coupling a large-scale image reconstruction problem with a nonlinear parameter estimation problem. Our main contributions are: 1. Two complementary realizations — AltMin and VarPro — that couple image reconstruction with parameter estimation while maintaining bounded mem-ory through Krylov recycling. Both are supported by a convergence guarantee to a stationary point of the joint objective (Theorem 4.2, Appendix A). NL-RMM-GKS FOR DYNAMIC AND STREAMING INVERSE PROBLEMS 23 

Fig. 7.3: Test 4. Effect of recycling window size on dynamic PAT reconstruction. (a) RRE vs. iteration for optical flow (solid) and ANISO-TV (dashed) across kmax ∈ { 10 , 15 , 20 , 25 }.Curves are tightly clustered, showing reconstruction is relatively insensitive to window size. (b) Parameter error converges rapidly for all settings. 

Fig. 7.4: Test 4. Visual comparison at three time points ( t = 0 , 4, 9) for kmax ∈ { 10 , 25 }.Columns compare ground truth, grid-search-only baseline, optical flow (OF), and ANISO-TV (TV). Optical flow preserves sharp block edges significantly better than ANISO-TV; differences between kmax = 10 and kmax = 25 are subtle. 

2. Streaming extensions that process sequential data blocks with basis recycling, enabling reconstruction from large-scale datasets without ever forming or storing the full forward operator. 3. A unified framework that generalizes MM-GKS, RMM-GKS, s-RMM-GKS, and MMGKS-OF as special cases, and naturally accommodates plug-in tem-poral regularization strategies including optical flow and anisotropic total variation for dynamic imaging problems. Numerical experiments in CT and PAT demonstrate that the recycling strategy successfully bounds memory growth — preventing the unbounded subspace expan-sion that afflicts standard MM-GKS — while simultaneously improving reconstruction 24 OKUNOLA T., PASHA M., KILMER M., NAGY J. DE STURLER E.       

> Table 7.2: Test 4: Dynamic PAT for blocks phantom (10 frames of 50 ×50, ptrue = 0 .1924,
> p(0) = 0 .25). Optical flow consistently outperforms ANISO-TV. Modest recycling windows (kmax = 15) provide a good balance between memory and accuracy.

Method Final RRE Param. Err. p∗

Optical Flow Regularization 

NL-RMM-GKS-OF ( kmax = 10) 0.1322 0.0060 0.1935 NL-RMM-GKS-OF ( kmax = 15) 0.1125 0.0060 0.1935 NL-RMM-GKS-OF ( kmax = 20) 0.1118 0.0058 0.1935 NL-RMM-GKS-OF ( kmax = 25) 0.1113 0.0065 0.1936 

Anisotropic TV Regularization 

NL-RMM-GKS-ANISO ( kmax = 10) 0.2767 0.0093 0.1942 NL-RMM-GKS-ANISO ( kmax = 15) 0.2631 0.0010 0.1943 NL-RMM-GKS-ANISO ( kmax = 20) 0.2625 0.0098 0.1943 NL-RMM-GKS-ANISO ( kmax = 25) 0.2645 0.0104 0.1944 quality by maintaining a high-quality compressed basis throughout iterations. Stream-ing variants offer a practical mechanism for handling large or sequentially acquired datasets, with the number of blocks N providing a controllable trade-off between memory, runtime, and reconstruction quality. For dynamic problems, optical flow regularization substantially outperforms anisotropic total variation when inter-frame motion is large and structured, while ANISO-TV remains a simpler and effective choice when frame-to-frame changes are small. Several directions remain open for future investigation: 

Convergence rates. Theorem 4.2 establishes convergence to a stationary point but does not characterize the rate of convergence. Establishing linear or sublinear convergence rates — potentially under additional assumptions on the objective — would provide a more complete theoretical foundation and guide practical parameter selection. 

Extension to multiple and distributed parameters. The current framework esti-mates a single global geometric parameter p per problem. Many practical systems require estimation of multiple independent parameters simultaneously — for exam-ple, per-angle perturbations in CT or per-sensor position errors in PAT. Extending NL-RMM-GKS to high-dimensional parameter spaces will require careful treatment of the Gauss-Newton system as np grows, potentially incorporating structure such as sparsity or low-rank constraints on the parameter vector. 

Real data validation. All experiments in this paper use simulated data with known ground truth. Validation on real CT and PAT acquisitions with genuine geometric un-certainties is an important next step, requiring careful treatment of model mismatch, non-Gaussian noise, and the absence of ground truth for quantitative evaluation. 

Learned initialization. The coarse grid search used for parameter initialization is effective but becomes expensive as np grows. A natural extension is to replace the grid search with a learned predictor — for example, a small network trained to predict geometric parameters from sinogram features — which could provide fast, reliable initialization even for high-dimensional parameter spaces, while the subsequent NL-RMM-GKS refinement retains the theoretical guarantees of the optimization-based approach. NL-RMM-GKS FOR DYNAMIC AND STREAMING INVERSE PROBLEMS 25 

Adaptive block partitioning for streaming. The current streaming implementation uses fixed randomly assigned blocks. An adaptive strategy that partitions data based on information content could improve reconstruction quality at a given N and reduce the number of passes required for convergence. 

Acknowledgments MP acknowledges support from NSF DMS 2410699, MK ac-knowledges support from NSF DMS 2410698, and JN acknowledges support from NSF DMS-2038118. Any opinions, findings, conclusions or recommendations expressed in this material are those of the authors and do not necessarily reflect the views of the National Science Foundation. 

REFERENCES [1] B. M. Afkham, J. Chung, and M. Chung , Learning regularization parameters of inverse problems via deep neural networks , Inverse Problems, 37 (2021), p. 105017. [2] S. Bonettini , Inexact block coordinate descent methods with application to non-negative matrix factorization , IMA Journal of Numerical Analysis, 31 (2011), pp. 1431–1452, https:// doi.org/10.1093/imanum/drq024, https://doi.org/10.1093/imanum/drq024, https://arxiv. org/abs/https://academic.oup.com/imajna/article-pdf/31/4/1431/1952671/drq024.pdf. [3] A. Buccini, M. Pasha, and L. Reichel , Modulus-based iterative methods for constrained 

ℓp − ℓq minimization , Inverse Problems, 36 (2020), p. 084001. [4] A. Buccini, M. Pasha, and L. Reichel , Linearized Krylov subspace Bregman iteration with nonnegativity constraint , Numerical Algorithms, 87 (2021), pp. 1177–1200. [5] A. Buccini and L. Reichel , Limited memory restarted ℓp − ℓq minimization methods using generalized Krylov subspaces , Advances in Computational Mathematics, 49 (2023), p. 26. [6] J. Chung, M. Chung, S. Gazzola, and M. Pasha , Efficient learning methods for large-scale optimal inversion design , Numerical Algebra, Control and Optimization, (2022), https: //doi.org/10.3934/naco.2022036. [7] J. Chung, M. Chung, and D. P. O’Leary , Learning regularization parameters for general-form Tikhonov regularization , Inverse Problems, 38 (2022), p. 045005. [8] J. Chung and S. Gazzola , Flexible Krylov methods for ℓp regularization , SIAM Journal on Scientific Computing, 41 (2019), pp. S149–S171. [9] M. Espanol and M. Pasha , An ℓ p Variable Projection Method for Large-Scale Separable Nonlinear Inverse Problems , arXiv preprint arXiv:2105.14155, (2021). [10] M. I. Espa˜ nol and M. Pasha , Variable projection methods for separable nonlinear inverse problems with general-form Tikhonov regularization , Inverse Problems, (2023). [11] S. Gazzola, P. C. Hansen, and J. G. Nagy , IR Tools: A MATLAB package of iterative regularization methods and large-scale test problems , Numerical Algorithms, 81 (2019), pp. 773–811. [12] G. H. Golub and V. Pereyra , The differentiation of pseudo-inverses and nonlinear least squares problems whose variables separate , SIAM Journal on Numerical Analysis, 10 (1973), pp. 413–432. [13] G. H. Golub and V. Pereyra , Separable nonlinear least squares: the variable projection method and its applications , Inverse Problems, 19 (2003), p. R1. [14] L. Kaufman , A variable projection method for solving separable nonlinear least squares prob-lems , BIT, 15 (1975), pp. 49–57, https://doi.org/10.1007/BF01932995. [15] S. Lan, M. Pasha, S. Li, and W. Shen , Spatiotemporal Besov priors for Bayesian inverse problems , Journal of the American Statistical Association, (2025), https://doi.org/10.1080/ 01621459.2025.2560688. [16] A. Lanza, S. Morigi, L. Reichel, and F. Sgallari , A generalized Krylov subspace method for ℓp − ℓq minimization , SIAM Journal on Scientific Computing, 37 (2015), pp. S30–S50. [17] A. Lanza, S. Morigi, L. Reichel, and F. Sgallari , A generalized krylov subspace method for 

ℓp-ℓq minimization , SIAM Journal on Scientific Computing, 37 (2015), pp. S30–S50, https: //doi.org/10.1137/140967982, https://doi.org/10.1137/140967982, https://arxiv.org/abs/ https://doi.org/10.1137/140967982. [18] J. Lindbloom, M. Pasha, J. Glaubitz, and Y. Marzouk , Priorconditioned sparsity-promoting projection methods for deterministic and bayesian linear inverse problems ,arXiv preprint arXiv:2505.01827, (2025). [19] C. Meng and J. Nagy , Numerical methods for CT reconstruction with unknown geometry parameters , Numerical Algorithms, 92 (2023), pp. 831–847. 26 OKUNOLA T., PASHA M., KILMER M., NAGY J. DE STURLER E. [20] T. Okunola, M. Pasha, M. Kilmer, and M. Freitag , Efficient dynamic image reconstruction with motion estimation , 2025, https://arxiv.org/abs/2501.12497, https://arxiv.org/abs/ 2501.12497. [21] M. Pasha, E. de Sturler, and M. E. Kilmer , Recycling MMGKS for large-scale dynamic and streaming data , arXiv preprint arXiv:2309.15759, (2023). [22] M. Pasha, S. Gazzola, C. Sanderford, and U. O. Ugwu , Trips-py: Techniques for regular-ization of inverse problems in python , Numerical Algorithms, (2024), pp. 1–38. [23] M. Pasha, A. K. Saibaba, S. Gazzola, M. I. Espa˜ nol, and E. de Sturler , A computa-tional framework for edge-preserving regularization in dynamic inverse problems , Elec-tronic Transactions on Numerical Analysis, 58 (2023), pp. 486–516. [24] A. Ruhe and P. A. Wedin , Algorithms for separable nonlinear least squares problems , tech. report, Stanford Univ., Calif. (USA). Dept. of Computer Science, 07 1974, https://www. osti.gov/biblio/4267344. [25] N. Srivastava, E. Mansimov, and R. Salakhutdinov , Unsupervised learning of video repre-sentations using lstms , International Conference on Machine Learning, (2015), pp. 843–852. [26] P. Tseng and S. Yun , A coordinate gradient descent method for nonsmooth separable mini-mization , Mathematical Programming, 117 (2009), pp. 387–423. [27] W. van Aarle, W. J. Palenstijn, J. De Beenhouwer, T. Altantzis, S. Bals, J. Baten-burg, and J. Sijbers , The astra toolbox: A platform for advanced algorithm development in electron tomography , Ultramicroscopy, 157 (2015), pp. 35–47, https://doi.org/10.1016/ j.ultramic.2015.05.002. 

Appendix A. Proof of Theorem 4.2.. 

We present the complete proof of Theorem 4.2. We first state the assumptions precisely, then establish a sequence of lemmas before combining them in the main proof. The extension to multiple inner iterations is given in Theorem A.11. 

A.1 Assumptions 

Assumption A.1 (Smoothness of H(p)). The matrix-valued function H(p) is twice continuously differentiable. There exist constants CH , C HH > 0 such that for all p ∈ Rnp and all i, j ∈ { 1, . . . , n p}:

(A.1) ∂H(p)

∂p i 2

≤ CH , ∂2H(p)

∂p i∂p j 2

≤ CHH .

Assumption A.2 (Bounded iterates and operators). The iterates remain bounded: there exist Ru, R p > 0 such that ∥u(k)∥2 ≤ Ru and ∥p(k)∥2 ≤ Rp for all k. The op-erators satisfy ∥H(p)∥2 ≤ CH for all ∥p∥2 ≤ Rp, ∥b∥2 ≤ Cb, and ∥Ψ∥2 ≤ CΨ for positive constants CH, C b, C Ψ.

Assumption A.3 (Gauss-Newton matrix conditioning). The damped Gauss-Newton matrix J(k,ℓ ) 

> p

satisfies 

(A.2) γmin I ⪯ J(k,ℓ ) 

> p

⪯ γmax I

for all k, ℓ and constants 0 < γ min ≤ γmax < ∞, where γmin ≥ μ is ensured by the damping parameter μ > 0.

These assumptions are standard in nonlinear optimization. Assumption A.1 holds for CT with angular perturbations and PAT with radial shifts. Assumption A.2 can be enforced via damping and trust-region strategies. Assumption A.3 is guaranteed by the positive damping parameter μ > 0. 

A.2 Proof Strategy The proof establishes that each outer iteration of Algo-rithm 4.2 produces sufficient descent in Jϵ,λ , and since the objective is bounded below, both gradient norms must be summable and hence converge to zero. The argument proceeds through six lemmas: NL-RMM-GKS FOR DYNAMIC AND STREAMING INVERSE PROBLEMS 27 1. Lemma A.4: Bound the spectral norm of the majorant Hessian Q(k) uni-formly over all iterations. 2. Lemma A.5: Show the image update produces descent of at least ∥∇ uJ ∥ 22/(2¯ μ). 3. Lemma A.6: Establish Lipschitz continuity of ∇pJ with explicit constant 

Lp.4. Lemmas A.7 and A.8: Show the Gauss-Newton direction is a descent direction with bounded norm. 5. Lemma A.9: Establish a positive lower bound αmin on the backtracking step size. 6. Lemma A.10: Show the parameter update produces descent of at least 

c1αmin γ−1max ∥∇ pJ ∥ 22.

A.3 Lemmas 

Lemma A.4 (Bound on majorant Hessian). Define the quadratic majorant Hes-sian 

(A.3) Q(k)(p) := H(p)⊤H(p) + λΨ⊤(P(k) 

> ε

)2Ψ,

where P(k) 

> ε

= diag((( Ψu (k))2 

> j

+ ε2)−1/4). Then 

(A.4) ¯μ := sup 

> k≥0

∥Q(k)(p(k))∥2 ≤ C2 

> H

+ λε C2 

> Ψ

< ∞.

Proof. By the triangle inequality and submultiplicativity: 

∥Q(k)(p(k))∥2 ≤ ∥ H(p(k))∥22 + λ∥Ψ∥22∥P(k) 

> ε

∥22 ≤ C2 

> H

+ λC 2 

> Ψ

· ε−1,

where we used Assumption A.2 and the fact that each diagonal entry of P(k) 

> ε

satisfies (( Ψu (k))2 

> j

+ ε2)−1/2 ≤ ε−1.

Lemma A.5 (Descent from image update). Define the quadratic majorant 

(A.5) Q(u, u(k); p(k)) = 12 u⊤Q(k)(p(k))u − u⊤H(p(k))⊤b + c(k),

where c(k) is independent of u, and let r(k) = ∇uJε,λ (u(k); p(k)). If 

(A.6) u(k+1) = arg min  

> u∈range[ V(k),r(k)]

Q(u, u(k); p(k)),

then 

(A.7) Jε,λ (u(k+1) ; p(k)) ≤ J ε,λ (u(k); p(k)) − ∥r(k)∥22

2¯ μ .

Proof. Since r(k) ∈ range[ V(k), r(k)], the minimizer u(k+1) is at least as good as the exact line search along −r(k). The exact line search step size is 

α∗ = ∥r(k)∥22

(r(k))⊤Q(k)(p(k))r(k) ,

and substituting u(k) − α∗r(k) into (A.5) gives 

Q(u(k) − α∗r(k), u(k); p(k)) = Q(u(k), u(k); p(k)) − ∥r(k)∥42

2( r(k))⊤Q(k)(p(k))r(k) .28 OKUNOLA T., PASHA M., KILMER M., NAGY J. DE STURLER E. 

Since ( r(k))⊤Q(k)r(k) ≤ ¯μ∥r(k)∥22 (Lemma A.4), we obtain 

Q(u(k+1) , u(k); p(k)) ≤ Q(u(k), u(k); p(k)) − ∥r(k)∥22

2¯ μ .

The result then follows from the majorization property Jε,λ (u(k+1) ; p(k)) ≤ Q(u(k+1) , u(k); p(k)), the tangency condition Q(u(k), u(k); p(k)) = Jε,λ (u(k); p(k)), and the tangency of gra-dients ∇uQ(u(k), u(k); p(k)) = ∇uJε,λ (u(k); p(k)) = r(k).

Lemma A.6 (Lipschitz continuity of ∇pJ ). For fixed u with ∥u∥2 ≤ Ru, the gradient ∇pJϵ,λ (u, p) is Lipschitz continuous in p with constant 

(A.8) Lp = CH R2 

> u

+ CHH Ru(CHRu + Cb).

Proof. The regularization term does not depend on p, so it suffices to consider 

Jdata (u, p) = 12 ∥H(p)u − b∥22. The i-th gradient component is 

∇pJdata 

 

> i

=

 ∂H

∂p i

u

⊤

(H(p)u − b).

For p1, p2, adding and subtracting a cross term and applying Cauchy-Schwarz gives 

∇pJdata (u, p1) − ∇ pJdata (u, p2) 

> i

≤ T1 + T2,

where T1 = ∂H(p1) 

> ∂p i

u 2

∥r(p1) − r(p2)∥2 and T2 =

 ∂H(p1) 

> ∂p i

− ∂H(p2)

> ∂p i



u

> 2

∥r(p2)∥2.By Assumption A.1 and the mean value theorem: 

∥r(p1)−r(p2)∥2 ≤ CH Ru∥p1−p2∥2,

 ∂H(p1)

∂p i

− ∂H(p2)

∂p i



u

> 2

≤ CHH Ru∥p1−p2∥2,

and by Assumption A.2, ∥r(p2)∥2 ≤ CHRu + Cb. Substituting and taking the Euclid-ean norm over all i yields (A.8). 

Lemma A.7 (Gauss-Newton descent direction). The Gauss-Newton direction 

d(k) = −(J(k) 

> p

)−1g(k) satisfies, whenever g(k)̸ = 0:

(A.9) ⟨g(k), d(k)⟩ ≤ − 1

γmax 

∥g(k)∥22 < 0.

Proof. By Assumption A.3 and the spectral theorem for symmetric positive defi-nite matrices: 

⟨g(k), d(k)⟩ = −(g(k))⊤(J(k) 

> p

)−1g(k) ≤ − ∥g(k)∥22

λmax (J(k) 

> p

) ≤ − ∥g(k)∥22

γmax 

.

Lemma A.8 (Gauss-Newton bounded norm). The Gauss-Newton direction d(k) =

−(J(k) 

> p

)−1g(k) satisfies: 

(A.10) ∥d(k)∥2 ≤ 1

γmin 

∥g(k)∥2.NL-RMM-GKS FOR DYNAMIC AND STREAMING INVERSE PROBLEMS 29 

Proof. By Assumption A.3: 

∥d(k)∥2 ≤ ∥ (J(k) 

> p

)−1∥2∥g(k)∥2 = ∥g(k)∥2

λmin (J(k) 

> p

) ≤ ∥g(k)∥2

γmin 

.

Lemma A.9 (Lower bound on backtracking step size). Under backtracking line search with initial step ¯α > 0, reduction factor β ∈ (0 , 1) , and Armijo constant 

c1 ∈ (0 , 1) , the accepted step size satisfies α(k) ≥ αmin , where 

(A.11) αmin := min 

(

¯α, 2β(1 − c1)γ2min 

Lpγmax 

)

> 0.

Proof. By Taylor expansion and Lemma A.6: 

J (u(k), p + αd(k)) ≤ J (u(k), p) + α⟨g(k), d(k)⟩ + Lpα2

2γ2min 

∥g(k)∥22,

where we used Lemma A.8 to bound ∥d(k)∥2. If α fails the Armijo condition, com-paring with c1α⟨g(k), d(k)⟩ and using Lemma A.7 gives α > 2(1 − c1)γ2min /(Lpγmax ). Since backtracking reduces α by factor β before accepting, the accepted step satisfies the stated bound. 

Lemma A.10 (Descent from parameter update). One Gauss-Newton iteration with backtracking line search produces: 

(A.12) Jϵ,λ (u(k), p(k)) ≤ J ϵ,λ (u(k), p(k−1) ) − c1αmin 

γmax 

∥g(k)∥22.

Proof. By the Armijo condition and Lemmas A.7 and A.9: 

Jϵ,λ (u(k), p(k)) ≤ J ϵ,λ (u(k), p(k−1) ) + c1α(k)⟨g(k), d(k)⟩ ≤ J ϵ,λ (u(k), p(k−1) ) − c1αmin 

γmax 

∥g(k)∥22.

A.4 Main Proof 

Proof of Theorem 4.2. Define κu := (2¯ μ)−1 and κp := c1αmin γ−1max , both positive by Lemmas A.4 and A.9. 

Step 1: Per-iteration descent. Applying Lemma A.5 to the image update and Lemma A.10 to the parameter update at each outer iteration k:(A.13) 

Jϵ,λ (u(k), p(k)) ≤ J ϵ,λ (u(k−1) , p(k−1) ) − κu∥∇ uJ (u(k−1) , p(k−1) )∥22 − κp∥g(k)∥22.

Step 2: Summability. Since Jϵ,λ ≥ 0, telescoping (A.13) from k = 1 to K

gives 

κuK−1X

> k=0

∥∇ uJ (u(k), p(k))∥22 + κpKX

> k=1

∥g(k)∥22 ≤ J ϵ,λ (u(0) , p(0) ) < ∞.

Taking K → ∞ :(A.14) 

> ∞

X

> k=0

∥∇ uJ (u(k), p(k))∥22 < ∞ and 

> ∞

X

> k=1

∥g(k)∥22 < ∞.30 OKUNOLA T., PASHA M., KILMER M., NAGY J. DE STURLER E. 

Step 3: ∇uJ → 0. A non-negative sequence whose terms are summable must converge to zero, so (A.14) immediately gives lim  

> k→∞

∥∇ uJϵ,λ (u(k), p(k))∥2 = 0 .

Step 4: ∇pJ → 0. Recall g(k) = ∇pJ (u(k), p(k−1) ). Since ∥g(k)∥2 → 0 from (A.14), it remains to transfer this to ∇pJ (u(k), p(k)). By Lemma A.8 and the update rule: 

∥p(k) − p(k−1) ∥2 ≤ ¯αγmin 

∥g(k)∥2.

Applying Lemma A.6 and the triangle inequality: 

∥∇ pJ (u(k), p(k))∥2 ≤

 Lp ¯αγmin 

+ 1 



∥g(k)∥2 → 0.

A.5 Extension to Multiple Inner Iterations 

Theorem A.11 (Convergence with multiple inner iterations). Under Assump-tions A.1–A.3, if RMM-GKS performs mk ≥ 1 inner iterations and the parameter update performs maxiter p ≥ 1 inner Gauss-Newton iterations at each outer iteration 

k, then 

lim  

> k→∞

∥∇ uJϵ,λ (u(k), p(k))∥2 = 0 and lim  

> k→∞

∥∇ pJϵ,λ (u(k), p(k))∥2 = 0 .

Proof. Applying Lemma A.5 to each of the mk inner image iterations and tele-scoping: (A.15) J (u(k+1) , p(k)) ≤ J (u(k), p(k)) − κumk −1X

> i=0

∥∇ uJ (u(k,i ), p(k))∥22.

Applying Lemma A.10 to each of the maxiter p inner parameter iterations and tele-scoping: (A.16) J (u(k+1) , p(k+1) ) ≤ J (u(k+1) , p(k)) − κp

> maxiter p−1

X

> ℓ=0

∥g(k+1 ,ℓ )∥22.

Combining (A.15) and (A.16) and summing over k = 0 , . . . , K − 1: 

κuK−1X 

> k=0
> mk−1

X

> i=0

∥∇ uJ (u(k,i ), p(k))∥22 +κpK−1X

> k=0 maxiter p−1

X

> ℓ=0

∥g(k+1 ,ℓ )∥22 ≤ J (u(0) , p(0) ) < ∞.

Taking K → ∞ , both double sums are finite. Since u(k) = u(k, 0) appears in the first sum, ∥∇ uJ (u(k), p(k))∥2 → 0 by summability of non-negative terms. For the p-gradient, since ∥g(k+1 ,maxiter p−1) ∥2 → 0 by summability, the same Lipschitz argument as in Step 4 of the main proof gives 

∥∇ pJ (u(k+1) , p(k+1) )∥2 ≤

 Lp ¯αγmin 

+ 1 



∥g(k+1 ,maxiter p−1) ∥2 → 0.

Appendix B. Supplementary Material. Section B.1 collects algorithms that were omitted from the main text to save space. Section B.2 studies the effect of inner iteration count on streaming reconstruction quality. NL-RMM-GKS FOR DYNAMIC AND STREAMING INVERSE PROBLEMS 31 

B.1 Supporting Algorithms B.1.1 MM-GKS Algorithm Algorithm B.1 gives the full MM-GKS proce-dure [17, 16] for solving the regularized linear subproblem min 

> u

12 ∥Hu − b∥22 + λ

> n

X

> j=1

ϕε(( Ψu )j ),

given a fixed forward operator H and regularization parameter λ. This is the core inner solver called by all variants of NL-RMM-GKS. 

Algorithm B.1 MM-GKS 

Require: H , Ψ, b, u(0) , ϵ 

Ensure: Approximate solution u(k+1)  

> 1:

Generate initial basis Vℓ with V⊤ 

> ℓ

Vℓ = I 

> 2:

for k = 0 , 1, 2, . . . until convergence  

> 3:

s(k) = Ψu (k) 

> 4:

w(k) 

> ϵ

= (( s(k))2 + ϵ2)−1/2 

> 5:

P(k) 

> ϵ

= diag( w(k) 

> ϵ

)1/2 

> 6:

HV ℓ+k = QH RH 

> 7:

P(k) 

> ϵ

ΨV ℓ+k = QΨRΨ 

> 8:

Select λ(k) by GCV  

> 9:

z(k+1) = arg min z

" RH

√λ(k) RΨ

#

z −

"

Q⊤ 

> H

b0

# 22 

> 10:

u(k+1) = Vℓ+kz(k+1)  

> 11:

r(k+1) = H⊤(Hu (k+1) − b) + λ(k)Ψ⊤(P(k) 

> ϵ

)2Ψu (k+1)  

> 12:

r(k+1) ← r(k+1) − Vℓ+kV⊤

> ℓ+k

r(k+1)  

> 13:

vnew = r(k+1) /∥r(k+1) ∥2 

> 14:

Vℓ+k+1 = [ Vℓ+k, vnew ] 

> 15:

end for B.1.2 Enlarge and Compress Subroutines Algorithms B.2 and B.3 imple-ment the subspace management subroutines used by RMM-GKS and all its extensions. 

Enlarge expands the current basis Vkmin by appending new Golub-Kahan vectors until the basis reaches size kmax . Compress reduces the enlarged basis back to size 

kmin via a truncated SVD, retaining the kmin directions of greatest variance to carry forward to the next iteration. 

B.1.3 UPDATE-PARAM-VarPro Algorithm B.4 implements the variable projection parameter update used in the VarPro realization of NL-RMM-GKS (Sec-tion 4.2 of the main paper). It exploits the separable structure of the VarPro objective to compute a Gauss-Newton step directly in the parameter space, treating the image as implicitly defined by the current parameter estimate. 

B.2 Effect of Inner Iteration Count on Streaming Performance We in-vestigate how the number of inner MM-GKS iterations per block affects reconstruction quality and runtime in the streaming setting, using the Test 1 setup from Section 6.2 of the main paper (static Shepp-Logan CT, N = 4 blocks, ptrue = 0 .2421 ◦). We vary 32 OKUNOLA T., PASHA M., KILMER M., NAGY J. DE STURLER E. 

Algorithm B.2 Enlarge 

Require: H , Ψ, Vkmin , d, u(0) , ϵ, s, tol 1, λ fix 

Ensure: u (kmax ), λ (kmax ), Vkmax , RH , RΨ

1: for k = 0 , . . . , s − 1

2: s(k) = Ψu (k)

3: w(k) 

> ϵ

= (( s(k))2 + ϵ2)−1/2

4: P(k) 

> ϵ

= diag( w(k) 

> ϵ

)1/2

5: HV kmin +k = QH RH

6: P(k) 

> ϵ

ΨV kmin +k = QΨRΨ

7: if λfix given 

8: λ(k) = λfix 

9: else 

10: λ(k) = arg min λ Θ( λ)

11: end if 

12: z(k+1) = ( R⊤ 

> H

RH + λ(k)R⊤

> Ψ

RΨ)−1R⊤ 

> H

Q⊤ 

> H

d

13: u(k+1) = Vkmin +kz(k+1) 

14: r(k+1) = H⊤(Hu (k+1) − d) + λ(k)Ψ⊤(P(k) 

> ϵ

)2Ψu (k+1) 

15: r(k+1) ← r(k+1) − Vkmin +kV⊤

> kmin +k

r(k+1) 

16: vnew = r(k+1) /∥r(k+1) ∥2

17: Vkmin +k+1 = [ Vkmin +k, vnew ]

18: if ∥u(k+1) − u(k)∥2/∥u(k)∥2 ≤ tol 1

19: break 

20: end if 

21: end for 

22: u(kmax ) = u(k+1) 

23: Vkmax = Vkmin +k+1 

Algorithm B.3 Compress 

Require: V kmax , RH , RΨ, d, u, QH , k min , λ 

Ensure: V kmin 

1: W = χ(RH , RΨ, QH , d, λ )

2: eV = Vkmax W

3: unew = u − eV eV⊤u

4: unew = unew /∥unew ∥2

5: Vkmin = [ eV, unew ]the inner iteration count m ∈ { 5, 10 , 15 , 20 } and report final RRE, parameter error, and wall-clock time. 

Results. Figure B.1 shows that reconstruction quality improves as m increases from 5 to 10, with diminishing returns beyond m = 10. Specifically, RRE decreases from 0.2103 at m = 5 to 0.1453 at m = 10, and only marginally further to 0.1388 at 

m = 20, while runtime increases roughly linearly with m. Parameter estimation is largely insensitive to m across the tested range. These results support the choice of 

m = 10 inner iterations used throughout the main paper as a good balance between accuracy and computational cost. Table B.1 gives the full numerical summary. NL-RMM-GKS FOR DYNAMIC AND STREAMING INVERSE PROBLEMS 33 

Algorithm B.4 UPDATE-PARAM-VarPro 

Require: H (·), Ψ, b, p(k), V(k) 

> kmin

, ˆλ, maxiter p, c1, β, ¯ α

Ensure: p (k+1) 

1: for ℓ = 0 , 1, . . . , maxiter p − 1

2: Form Hℓ = H(p(k,ℓ )) and reduced basis ˆAℓ = HℓV(k)

> kmin

3: Compute VarPro residual: 

y(k,ℓ ) = arg min 

> y

" ˆAℓpˆλ ˆBℓ

#

y −

b0

 22

, ˆu(k,ℓ ) = V(k) 

> kmin

y(k,ℓ )

4: Compute VarPro gradient via implicit differentiation [12, 13]: 

g(k,ℓ ) = ∇pJε, ˆλ(ˆ u(k,ℓ ), p(k,ℓ ))

5: Form damped Gauss-Newton matrix J(k,ℓ ) 

> p

and compute direction: 

d(k,ℓ ) = −(J(k,ℓ ) 

> p

)−1g(k,ℓ )

6: Backtracking line search with Armijo condition ( c1, β, ¯ α) to find α(k,ℓ )

7: p(k,ℓ +1) = p(k,ℓ ) + α(k,ℓ )d(k,ℓ )

8: end for 

9: p(k+1) = p(k, maxiter p)

Table B.1: Effect of inner iteration count on s-NL-RMM-GKS ( N = 4 blocks, Test 1 setup, 

ptrue = 0 .2421 ◦). 

Inner Iter Time (min) Peak Mem. (GB) Final RRE Param. Err p∗ (°)5 0.22 0.088 0.1699 0.0490 0.2302 10 0.41 0.118 0.1351 0.0071 0.2404 15 0.67 0.152 0.1400 0.0066 0.2405 20 0.89 0.188 0.1453 0.0053 0.2408 34 OKUNOLA T., PASHA M., KILMER M., NAGY J. DE STURLER E. 

> (a) RRE vs. outer iteration for varying inner iterations (5, 10, 15, 20)
> (b) Visual reconstructions comparing ground truth with results from 5, 10, 15, 20 inner iterations

Fig. B.1: Effect of inner iteration count m on streaming NL-RMM-GKS ( N = 4 blocks, Test 1 setup). (a) shows diminishing returns beyond 10 inner iterations, with all configurations converging to similar final RRE. (b) confirms that visual quality is comparable across settings, suggesting 10 iterations provides the best speed-accuracy balance.